class UserController {
  constructor() {}

  // * Get profile details
  async getDetails(req, res, next) {
    try {
      // *  Get profile details
    } catch (err) {
      next(err);
    }
  }
}

export default UserController;
